# pi-service/requirements.txt
# Azure Pi Service Dependencies - NO hardware libraries!

# Core FastAPI Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Authentication & Security (same as main backend)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Database (same as main backend)
sqlalchemy==2.0.21
psycopg2-binary==2.9.7

# HTTP Client for Pi Communication
httpx==0.25.0
aiofiles==23.2.1

# Configuration & Environment
python-dotenv==1.0.0
pydantic==2.4.2
pydantic-settings==2.0.3

# WebSocket Support
websockets==11.0.3

# Date/Time utilities
python-dateutil==2.8.2

# NOTE: This file is for AZURE SERVICE only
# Your pi_requirement.txt is for the physical Pi hardware
# NO camera libraries (picamera2, libcamera, opencv) needed here!