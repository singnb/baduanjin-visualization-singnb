# ============================================================================
# requirements-docker.txt - Safe packages that won't upgrade numpy
# 
# EXCLUDED (installed separately with constraints):
# - numpy, scipy, pandas, matplotlib, seaborn
# - torch, torchvision, opencv-python-headless
# - mmengine, mmcv-full, mmdet, mmpose 
# ============================================================================

# Core FastAPI and database (safe - don't depend on numpy)
fastapi==0.115.12
uvicorn[standard]==0.34.2
sqlalchemy==2.0.40
psycopg2-binary==2.9.10

# Authentication (safe - no numpy dependencies)
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
jwt==1.3.1

# File handling (safe)
python-multipart==0.0.20
aiofiles==24.1.0

# Validation and settings (safe)
pydantic[email]==2.11.3
pydantic-settings==2.9.1
email-validator==2.2.0
python-dotenv==1.1.0

# Azure integration (safe)
azure-storage-blob==12.25.1

# HTTP and utilities (safe)
requests==2.28.2
httpx==0.24.1
httpcore==0.9.1
Jinja2==3.1.6

# Audio processing (safe - no numpy dependencies)
SpeechRecognition==3.14.3
googletrans==4.0.0rc1
gTTS==2.5.4
pydub==0.25.1

# Basic utilities (safe)
tqdm==4.65.2
decorator==5.1.1
proglog==0.1.12

# Video processing - using older versions compatible with numpy 1.24.3
moviepy==1.0.3
imageio==2.31.6
imageio-ffmpeg==0.4.9